# Docker profile — runs inside container with local filesystem
# Data and output mounted as volumes at /data and /output
include "application.conf"

app {
  spark {
    master = "local[*]"
  }

  paths {
    data-dir = "/data/ml-latest"
    output-dir = "/output"
  }
}
