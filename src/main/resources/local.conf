# Local development profile
# Uses local HDFS or filesystem paths
include "application.conf"

app {
  spark {
    master = "local[*]"
  }

  paths {
    data-dir = "hdfs://localhost:9000/ml-latest"
    output-dir = "hdfs://localhost:9000/ml-latest"
  }
}
